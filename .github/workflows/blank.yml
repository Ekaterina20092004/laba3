name: TESTS

on: [push, pull_request]
jobs:
  work_test:
      runs-on: ubuntu-latest
      steps:
        - name: Setup Python
          uses: actions/setup-python@v4  
          with:
            python-version: '3.10'
        - name: Run Python unit tests
          run: python3 -u -m unittest test.TestAddFunction
